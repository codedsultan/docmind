---
# ⚠️  PLACEHOLDER — DO NOT COMMIT THIS FILE UNENCRYPTED.
#
# Fill in real values, then encrypt with:
#   ansible-vault encrypt infra/ansible/inventories/staging/group_vars/all/vault.yml
#
# The pre-commit hook (infra/ansible/scripts/preflight.sh) will refuse to
# let you commit this file unless it starts with $ANSIBLE_VAULT;.

vault_ghcr_username: "CHANGEME"
vault_ghcr_token: "CHANGEME"

vault_pg_app_innermind_password: "CHANGEME"
vault_redis_password: "CHANGEME"
vault_innermind_jwt_secret: "CHANGEME"

vault_staging_server_vpn_ip: "CHANGEME"
